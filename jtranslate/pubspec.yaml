name: jtranslate
description: "A JSON based translation and textblock library."
version: 1.0.1
homepage: https://github.com/BenediktJSchlegel/jtranslate

platforms:
  android:
  ios:
  windows:

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter: